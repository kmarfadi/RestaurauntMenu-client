# API Configuration
# Base URL for the restaurant menu API server
# Default: http://localhost:3003 (if not set)
VITE_API_BASE_URL=http://localhost:3001

# Note: In Vite, all environment variables must be prefixed with VITE_ to be accessible in the browser
# The client will fallback to http://localhost:3003 if VITE_API_BASE_URL is not set
